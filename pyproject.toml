[project]
name = "python-rtlamr"
version = "0.1.0"
description = "Python implementation of RTLAMR-go"
dependencies = [
  "numpy>=1.24",
  "pyrtlsdr==0.3.0",
  "pyusb>=1.3.1",
  "setuptools",
]

[project.optional-dependencies]
dev = [
  "pytest",
]

[tool.black]
line-length = 80
